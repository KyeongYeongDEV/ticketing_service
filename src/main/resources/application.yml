spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  data:
    redis:
      host: localhost
      port: 6379

payment:
  toss:
    secret-key: test_sk_d46qopOB8969mO1QvYoL3ZmM75y0
    url: https://api.tosspayments.com/v1/payments/confirm